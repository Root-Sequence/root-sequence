# Earlier Public Seed source

These files powered the superseded 11-page Public Seed v0.1 candidate saved on
2026-09-16. They are retained as a clearly labelled historical source, not as
the current website and not as deployment input.

The current canonical page is [`../index.html`](../index.html), and the current
publisher is [`../build.py`](../build.py).

Source links that refer to paths retired after this edition are pinned to the
last published commit that contained them. This keeps the historical record
readable without turning those paths back into current repository structure.
